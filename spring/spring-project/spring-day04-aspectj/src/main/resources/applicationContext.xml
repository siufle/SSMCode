<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/aop
       https://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--管理目标类-->
    <bean id="userService" class="com.xq.service.impl.UserServiceImpl"></bean>

    <!--管理切面类-->
    <bean id="myAspect" class="com.xq.aspect.MyAspect"></bean>

    <!--配置目标类和切面类-->
    <aop:config>
        <!--配置切点  execution 第一个参数为切点方法的返回值类型 *表示任意类型
                              第二个参数表示切点方法绝对路径()内参数表示方法参数类型 ..表示任意类型-->
        <aop:pointcut id="p1" expression="execution(* com.xq.service.impl.UserServiceImpl.deleteUser(..))"/>
        <aop:pointcut id="p2" expression="execution(* com.xq.service.impl.UserServiceImpl.findAll(..))"/>
        <aop:pointcut id="p3" expression="execution(* com.xq.service.impl.UserServiceImpl.updateUser(..))"/>
        <aop:pointcut id="p4" expression="execution(* com.xq.service.impl.UserServiceImpl.addUser(..))"/>
        <!--配置切面 产生代理对象 将通知应用到切点方法上-->
        <aop:aspect ref="myAspect">
            <!--配置前置标签-->
            <aop:before method="checkPrivilege" pointcut-ref="p1"></aop:before>
            <!--配置后置标签-->
            <aop:after-returning method="PrintLog" pointcut-ref="p2"></aop:after-returning>
            <!--配置环绕通知-->
            <aop:around method="around" pointcut-ref="p3"></aop:around>
            <!--配置抛出异常通知-->
            <aop:after-throwing method="throwing" pointcut-ref="p4"></aop:after-throwing>
            <!--配置最终通知-->
            <aop:after method="after" pointcut-ref="p4"></aop:after>
        </aop:aspect>
    </aop:config>
</beans>